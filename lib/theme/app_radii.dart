/// Tokens de rayons (border-radius) du design Asfar Premium.
///
/// Échelle issue du prototype HTML — utiliser ces constantes plutôt que des
/// valeurs littérales pour assurer la cohérence visuelle.
class AppRadii {
  AppRadii._();

  /// Petit rayon — chips secondaires, badges, micro-éléments.
  static const double sm = 10;

  /// Rayon medium — boutons, inputs, listrows.
  static const double md = 14;

  /// Rayon large — cards, sheets.
  static const double lg = 20;

  /// Rayon extra-large — hero cards, modals.
  static const double xl = 28;

  /// Pill / circulaire complet — avatars, dots, status badges.
  static const double pill = 999;
}
